{
  "name": "@animando/redux-saga-callback",
  "version": "1.0.5",
  "description": "Turns a callback into a redux saga",
  "repository": "git@github.com:gking2224/redux-saga-callback.git",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:ts": "rm -rf build",
    "clean": "npm run clean:ts",
    "build": "npm run clean && npm run build:ts",
    "build:ts": "tsc -p ."
  },
  "keywords": [],
  "author": "Graham King",
  "license": "MIT",
  "dependencies": {
    "redux-actions": "^2.6.5",
    "redux-saga": "^1.1.3"
  },
  "peerDependencies": {
    "redux-actions": "^2.6.5",
    "redux-saga": "^1.1.3"
  },
  "devDependencies": {
    "@types/redux-actions": "^2.6.1"
  }
}
